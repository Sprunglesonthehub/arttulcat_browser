// This is a placeholder file for Firefox branding preferences that the installer expects.
// It's being used to define Arttulcat Browser's default homepage.
// Replace with actual Arttulcat-specific preferences if needed for your custom build.

// Set the default homepage for Arttulcat Browser
pref("browser.startup.homepage", "https://arttulos.org/search.html");

// Add other essential default preferences if you know them, e.g.:
// pref("app.update.channel", "release");
// pref("app.update.auto", true);
